# Outcomes-Driven Development (ODD)

The portable core of ODD: universal governance and canon, plus the software-development vertical. ODD treats outcomes as primary, artifacts as ephemeral, and verification as mandatory. AI accelerates execution; governance preserves trust.

This repository is the write model for the `odd://` authority. Authoring happens here via PRs; consumers query the read model through oddkit (`https://oddkit.klappy.dev`). To list the documents in this collection: oddkit `catalog` with `knowledge_base_url=https://github.com/klappy/outcomes-driven-development`.

Extracted from `klappy/klappy.dev` per D0002 (the Canon Storage Model) — the bifurcation is a write-path re-shard; all existing URIs are grandfathered opaque keys and resolve unchanged through the read model.

License: not yet declared (all rights reserved). Licensing here is deliberate — licensed, never assigned.
